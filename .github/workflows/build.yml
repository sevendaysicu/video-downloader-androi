name: Build Android APK
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-22.04 
    steps:
      - name: 1. 拉取仓库代码
        uses: actions/checkout@v4

      - name: 2. 安装并配置 Java 8 环境
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '8' 

      - name: 3. 配置 Python 环境
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 4. 安装 Linux 系统底层编译依赖
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential ccache git zlib1g-dev libffi-dev libssl-dev libncurses5-dev libgdbm-dev libnss3-dev libsqlite3-dev libreadline-dev tk-dev libgmp-dev libmpfr-dev libmpc-dev libasound2-dev patchelf unzip
          sudo apt-get install -y lib32stdc++6 lib32z1

      - name: 5. 注入官方历史 tools 目录，并全自动签署 2026 年全部最新官方协议锁
        run: |
          mkdir -p /home/runner/.buildozer/android/platform/android-sdk
          cd /home/runner/.buildozer/android/platform/android-sdk
          
          # 下载自带老路径的历史工具包
          wget -q https://dl.google.com/android/repository/sdk-tools-linux-4333796.zip
          unzip -q sdk-tools-linux-4333796.zip
          
          # 【核心绝杀】呼叫老版 sdkmanager，用管道流全自动疯狂按下 'y'，强行签掉包含 Build-Tools 37 在内的所有新老授权协议！
          yes | tools/bin/sdkmanager --licenses || true

      - name: 6. 安装 Buildozer 核心打包工具
        run: |
          python -m pip install --upgrade pip
          pip install --upgrade virtualenv buildozer sh Cython

      - name: 7. 诱骗并强制物理同步所有潜在版本的 build-tools (全面兼容第 37 版工具链)
        run: |
          # 1. 后台诱骗运行 45 秒，让签完协议的工具链无阻碍全量下载
          buildozer android debug > /dev/null 2>&1 &
          sleep 45
          pkill -f buildozer || true
          pkill -f python || true
          
          # 2. 【路径大包围】管它下载下来的是 33.0.0 还是 37.0.0，通通打包将文件提取到母目录下
          SDK_DIR="/home/runner/.buildozer/android/platform/android-sdk"
          mkdir -p $SDK_DIR/build-tools
          
          # 遍历所有被下载到后缀目录里的真家伙，把里面的 aidl 全局无损平铺出来
          find $SDK_DIR/build-tools/ -mindepth 2 -maxdepth 2 -exec cp -r {}/ $SDK_DIR/build-tools/ \; 2>/dev/null || true
          
          # 注入环境变量最高指挥部
          echo "$SDK_DIR/build-tools" >> $GITHUB_PATH

      - name: 8. 启动官方 Buildozer 引擎执行真正的最终闭包编译
        run: |
          chmod -R 777 .
          # 协议已签、第 37 版依赖全部顺利下完并平铺，老代码将一路绿灯直接通过校验！
          buildozer -v android debug

      - name: 9. 完美上传打包好的 APK 产物
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: VideoDownloader-APK
          path: bin/*.apk
